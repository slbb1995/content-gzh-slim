# Content GZH Slim 工作规则

## 2026-09-09 封面风格更新

当前采用 PR #7 的三种风格：麦肯锡商业咨询（`consulting`）、实拍写实杂志风（`real-photo`）、复古图纸聚焦风（`retro-blueprint`）。本段替代下方历史授权中的旧风格列表；旧记录保留，不再作为当前选项。写作流程与两次真人 Gate 不变。Windows 兼容验收范围见 README，不把 macOS 回归通过等同于 Windows 全量通过。

## 2026-09-08 当前交付：封面接入与独立验收

用户已明确授权将已确认的封面能力接入 GitHub 的 `slbb1995/content-gzh-slim`，进行子 Agent 独立 Review、隔离合成端到端测试和实际问题修复，验证后提交推送并更新本地完整包。此授权覆盖下方历史分阶段与本地“不提交推送”的限制。合成测试可由代理操作两次 Gate，但只能用于明确标记的隔离测试 Run，不能替代真实生产内容的真人批准；不写现有客户知识库、不发布内容。日常流程仍为一次文字选择一种风格，生成一张并保存；飞书自动插图不在本期范围。

## 2026-09-07 用户授权补充：轻量封面（v2）

用户已批准“文章保存后，一次文字推荐/选风格，生成一张并保存”，并授权本地实现、内部三风格视觉测试和完整包安装。此项优先于历史分阶段限制：预算增为 1 个公开入口、6 个内部 Skill、2 个正文真人 Gate、1 个正式 Context、0 Reviewer。新增 `content-gzh-cover`，三种风格为咨询、复古油墨、光栅科技。模型自主构图与提炼封面短标题，不额外要求 Prompt/短标题/保存确认，不建封面状态机或多 Agent 链。仅补必要写回兼容与一份封面记录。测试三图不属于普通用户流程。本轮不提交、推送或发布；飞书自动插图不在本期范围。


## 1. 真相源与阶段授权

- `CONTENT-GZH-SLIM-SPEC.md` 是最高真相源。
- `SLIM-COMPASS.md` 只能压缩和导航，不得发明新要求。
- `project-state.json` 只记录真实状态；`next_phase` 不代表已经授权。
- 每次只执行用户明确授权的一个阶段。
- 没有明确的 `EXECUTE_PHASE: Pn`，只允许读取、评估和更新方案，不得实现下游阶段。

## 2. 仓库边界

- 本仓库独立于 `~/.codex/skills`、Content V2、现役公众号 V1 和任何客户知识库。
- 未到构建安装阶段，不得写入 `~/.codex/skills`。
- 不修改 `shu-gongzhonghao-v1`、`content-slim`、`zsk-router` 或客户 01—05 资产。
- 客户资料、IP 语料、真实正文和测试隐私数据不得提交到本仓库。
- 测试只使用脱敏 fixture 或独立私有临时知识库。

## 3. 反膨胀原则

- 总量预算：1 个公开入口、6 个内部 AI Skill、2 个真人 Gate、1 个正式 Context Pack、0 Reviewer；独立测试 Agent 不进入日常用户流程。
- 保存、状态、检索、路径校验和后端读写属于确定性 Runtime，不额外创建 Skill。
- 不增加第二层 Packet、Source Pack、Writing Packet、P0—P9 后处理链或多 Agent 审核链。
- 理论风险先登记；没有真实失败证据，不增加状态、Schema、Artifact、Gate、Skill 或用户字段。
- 每阶段新增内容超过 Master SPEC 预算时立即停止，先请求用户决定。
- 每阶段只允许 `1 次 PM Review → 最多 1 轮最小修复 → 1 次复检`；禁止第二轮自动修复。
- 一轮修复后，核心结果可用且无硬阻断时，记录 `deferred_issues` 并继续，优先把 P1—P6 完整链跑出来。
- 一轮修复后仍有串库/串 IP、越权、数据损坏、Gate 绕过、敏感信息入 Git 或核心结果不可用时，停止并交给用户决定；不得无限 Review。
- P6 前不为审美、重构、理论风险或未来兼容反复打磨；P6 后只按真实任务稳定复现的问题修复。

## 4. 内容与来源边界

- 05 只支撑本次指定 IP；03 支撑业务事实；04 同行保留角度、通用观点、场景和论证的内容价值，结构资产支撑组织方法。按 Master SPEC 的 2026-09-07 补充做一段想法启动的语义选材。
- 候选素材不得升级为确认事实。
- IP 资料不足不阻断：提醒并继续，只用已确认片段；必要时降级为无 IP 写法。
- 不得把虚构经历、客户案例、结果数字或第一人称事实写成真实内容。
- 对标只迁移钩子、结构、论证和节奏，不迁移同行身份、经历、案例、专属数据和识别性原句。

## 5. Git 与交付

- 每阶段独立提交，提交信息必须说明阶段和真实范围。
- Gate Review 只审核当前阶段 Diff，不顺手修未来阶段。
- 阶段回执必须单列 `deferred_issues`；它们不是当前阶段继续修复的默认授权。
- 未经明确授权，不合并、打 Tag、构建、安装或发布。
- 报告必须区分：文件已创建、测试已通过、已推送、已安装、已保存、已发布。
