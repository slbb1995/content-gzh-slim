# Content GZH Slim 全周期产品与开发 SPEC

> 文档状态：P0 候选真相源，待用户 Review
>
> 产品名称：`content-gzh-slim`
>
> 目标：把现役公众号 V1 中真正有用的内容生产逻辑抽出来，形成一套可服务多个知识库、多个项目和多个 IP 的标准化公众号产品。

---

## 0. 文档权威与执行规则

### 0.1 真相源

本文件是仓库最高真相源。`SLIM-COMPASS.md`、阶段卡、代码、Schema、测试和 Review 均不得新增本文件未授权的产品能力。

冲突优先级：

```text
用户本轮明确决定
> 本 Master SPEC
> 当前阶段卡
> Skill 说明
> Runtime 与 Schema
> 测试 fixture
> 历史 V1 行为
```

### 0.2 当前授权

P0 只允许：

- 建立独立私有仓库；
- 编写并 Review 本 SPEC；
- 定义 Skill、Runtime、阶段和测试边界；
- 不实现、不安装、不迁移、不修改客户内容。

### 0.3 阶段规则

- 每次只执行用户明确授权的一个阶段。
- `next_phase` 不是授权。
- 每阶段完成后必须停下，等待独立 Review 或用户决定。
- 理论风险不得自动扩成当前阶段机制。
- 修复只修当前 Gate 的真实阻断，不顺便建设未来能力。

### 0.4 完整度优先与单轮修复预算

本项目优先把 P1—P6 的完整业务骨架跑通，再使用真实任务集中发现和修正问题。不得为了追求阶段内“完美”反复 Review、反复修补，重新把 Slim 做成长链。

每个阶段固定采用：

```text
开发候选
→ PM Review
→ 最多 1 轮最小修复
→ 1 次复检
→ 通过、带已知问题前进，或升级给用户判断
```

规则：

- 每阶段自动修复预算固定为 1 轮；不得自行开启第二轮修复。
- 一轮修复后，核心阶段结果可用且不存在硬阻断时，允许带着已记录的非关键问题进入下一阶段。
- 非关键问题进入阶段回执的 `deferred_issues`，优先留到 P6 真实任务测试后统一判断；不为理论风险提前加机制。
- 一轮修复后仍存在硬阻断时，不得“差不多算了”，也不得继续自动修；必须停止并交给用户决定范围、降级或延期。
- PM 不得对同一提交范围反复提出新的审美、重构、可选增强和未来兼容要求。
- P6 以前以链路完整度为主；P6 以后只修真实样本稳定复现、确实影响业务结果的问题。

硬阻断只包括：

1. 当前阶段核心业务结果无法完成；
2. 串知识库、串 IP、越权读取或隐私泄露；
3. 覆盖、丢失或错误保存已确认内容；
4. 跳过 Gate A / Gate B 或版本错绑；
5. 凭据、客户资料或其他敏感内容进入 Git；
6. 实现越过当前阶段或违反 Master SPEC 的冻结边界。

---

## 1. 背景与问题

### 1.1 现役 V1 的价值

现役 `shu-gongzhonghao-v1` 已验证下列业务能力有价值：

- 从选题、对标和用户想法启动；
- 读取不同 Profile，使同一流程服务石榴爸爸、树老师等不同 IP；
- 深度拆解爆款文章；
- 区分干货型和话题型写作；
- 生成人工确认的正文和标题；
- 保存到知识库；
- 可继续生成分发物料。

### 1.2 现役 V1 为什么变重

V1 把以下职责持续堆进同一条主链：

- 大量前置合同和 Profile 文件读取；
- Reference Snapshot、Source Pack、Writing Packet 等重复中转；
- 多来源兼容与修复逻辑；
- 双标题引擎、评分和仲裁；
- AI 味检查、去 AI 味、配图、视觉 QC、写回；
- P0—P9 后处理；
- 草稿箱、反馈学习、发布归档；
- 大量状态、收据、报告和恢复入口。

问题不只是文件多，而是每篇文章都可能被迫携带与当前任务无关的读取和分支，导致上下文膨胀、注意力失焦和维护成本上升。

### 1.3 本项目不修 V1

本项目采用平行重建：

- V1 冻结为对照组；
- 不共享旧 Run、State、中间包和 P0—P9；
- 只提炼被真实使用证明有价值的业务能力；
- 新系统测试稳定前，不替换现役入口。

---

## 2. 产品结论

### 2.1 一句话定义

`content-gzh-slim` 是一套通用公众号内容生产引擎。用户每次通过自然语言提示词注入知识库和 IP，系统按需读取少量 05、03、04 与显式对标，经过两次真人确认，生成正文、标题并保存回指定知识库。

### 2.2 第一性原理

```text
05 IP 决定谁在说、说给谁、用什么立场和语气说
03 业务知识决定有什么真实内容可以讲
04 内容方法决定怎样组织、怎样吸引和怎样推进
显式对标决定本篇可以借鉴的爆款机制
用户确认决定方向和最终成稿
```

### 2.3 标准化方式

- 流程标准化，不把客户内容写死进 Skill。
- 知识库和 IP 最终都是 Run 参数；宿主可以保存经过真人确认的默认 binding，但默认值不限制本次显式选择。
- 同一知识库可以拥有多个 IP。
- 同一个客户可以拥有多个知识库、项目和 IP。
- 每个 Run 只冻结一个知识库和一个选定 IP；primary 只表示默认。无 IP 时显式或通过已确认配置冻结 `none`。
- 未来多讲述者文章必须另行设计，不在第一版默认支持。

---

## 3. 产品目标与非目标

### 3.1 第一版必须完成

1. 一个日常自然语言入口。
2. 运行时显式指定飞书/Obsidian 知识库，或采用已确认的工作流默认 binding。
3. 运行时显式指定任意 active IP、`无IP`，或采用已确认的默认 IP。
4. 有 IP 时按 05→03→04 顺序按需读取。
5. 无 IP 时跳过 05，仅使用 03、04 和当前输入。
6. 有显式对标时完成逐篇深度拆解。
7. 无明确方向时从知识库生成 3 个候选方向，纳入 Gate A。
8. Gate A 确认方向。
9. 生成唯一 Article Context Pack。
10. 生成并修改公众号正文。
11. 生成 Top 3 和推荐标题。
12. Gate B 一次确认正文、标题和保存目标。
13. 保存回本次指定知识库并回读。
14. 保存后可选生成全平台分发包。
15. 记录本次实际读取了哪些资产和多少内容。

### 3.2 第一版明确不做

- 不创建或维护知识库；知识库由 `zsk-router` 上游负责。
- 不执行资料入库，不修改 01—05。
- 不全量扫描 Vault、飞书空间或整个知识库。
- 不自动运行 AI 味检测、质量评分和独立 Reviewer。
- 不自动生图、生成口播、同步草稿箱或正式发布。
- 不学习用户改稿，不更新 Profile。
- 不复用 V1 Run、Source Pack、Writing Packet、双标题仲裁和 P0—P9。
- 不把一个客户永久限制为单一知识库或单一 IP；可选默认绑定必须可明确覆盖。
- 不把保存、草稿箱和正式发布混成一个状态。

---

## 4. 与 ZSK 知识库体系的关系

### 4.1 上游职责

`zsk-router` 负责：

- 建立飞书或 Obsidian 知识库；
- 登记来源；
- 把业务知识整理进 03；
- 把同行内容和内容方法整理进 04；
- 把 IP 资料整理进 05；
- 维护来源、隐私、主体和版本边界。

### 4.2 本项目职责

`content-gzh-slim` 只消费已经存在的知识库：

- 不调用 ZSK 写入 03/04/05；
- 不把文章生成过程中产生的猜测回写为知识资产；
- 只通过知识库 Manifest 或等价自描述入口定位 03、04、05 和成稿区；
- 最终正文只写入 Manifest 授权的成稿区。

### 4.3 后端

第一版支持两类后端：

- Obsidian：本地知识库路径和受控成稿目录；
- 飞书：知识空间或文档节点引用和受控成稿节点。

后端差异由 Adapter 处理，AI Skill 不拼绝对路径、飞书 token 或凭据。

---

## 5. 用户入口合同

### 5.1 标准提示词

```text
请按 content-gzh-slim 执行

知识库：
- 客户知识库名称 / 飞书链接 / Obsidian 路径

IP：
- IP 名称 / 无IP

选题：
-

对标：
-

我的想法：
-
```

### 5.2 必填与选填

解析后必填：

- `knowledge_base`：必须唯一解析到一个飞书或 Obsidian 知识库；请求可省略，但只能由已确认默认值补齐。
- `ip`：一个名字或 `none`；请求可省略，但只能按“工作流默认 → primary → 唯一 active”补齐，仍不唯一就要求选择。

选填：

- `topic`
- `references`：0—5 篇。
- `user_thoughts`
- `must_keep`
- `must_avoid`
- `target_audience_override`
- `article_length_preference`

### 5.3 IP 名称规则

- 用户只填写 IP 名称，不填写 `05/名称`。
- Runtime 在本次知识库的 05 中解析名字。
- 解析到唯一目标时继续。
- 同名多目标或主体冲突时要求用户选择，不能混读。
- IP 不存在或资料有限时不阻断：提示后进入 `limited` 或 `unused`。

### 5.4 无方向模式

当 `topic` 和 `user_thoughts` 均不足以形成方向时：

- 系统仍按当前知识库和 IP 受控检索；
- Gate A 展示 3 个候选方向；
- 用户选择、修改或不采用；
- 选择动作属于 Gate A，不新增第三个前置 Gate。

---

## 6. Run 身份与隔离

### 6.1 每个 Run 冻结

- `knowledge_base_ref`
- `knowledge_base_backend`
- `knowledge_base_manifest_revision`
- `ip_name` 或 `none`
- `ip_resolved_ref` 或 `null`
- 用户原始选题、对标集合和想法

### 6.2 不永久死绑

- 系统不维护“一客户只能有一个知识库”的永久规则。
- 公共 Registry 只保存经过真人确认的可覆盖默认值，不让一个客户或 primary 垄断后续 Run。
- 下一篇文章可以指定另一个知识库或另一个 IP。
- 同一知识库可以连续选择不同 IP。
- 知识库或 IP 在当前 Run 中改变时，必须新建 Run，不能在旧 Context Pack 上替换。

### 6.3 任务复用

相同知识库、IP、原始输入和参考集合重试时，应恢复同一正常 Run；机械失败不得新建 Run 掩盖问题。

---

## 7. 03 / 04 / 05 来源角色

### 7.1 05：IP 资料

05 只回答“谁在说”：

- 身份、定位、受众；
- 核心立场与稳定判断；
- 表达风格和禁区；
- 业务方向、CTA 和承诺边界；
- 原话、故事、近期动作和候选素材。

确认事实、运营设定和候选素材必须保持分层。候选素材不得自动升级为真实经历。

Profile 正文按章节语义装配为身份事实、表达方式、专业判断、读者理解、真实经历和业务边界；不得再按“前若干条 bullet”混为通用片段。专业判断可以形成 IP 口吻，第一人称经历仍必须有明确确认片段。

### 7.2 03：业务知识

03 只支撑当前知识库主体的业务事实：

- 产品、服务、流程、政策、案例、数据、客户问题；
- 当前项目可公开的业务判断；
- 可用于本篇的事实和边界。

03 为空时可以一般化或条件化表达，不得用 04 或模型常识冒充客户事实。

### 7.3 04：内容与方法

04 分两类：

- `peer_content_asset`：通用选题、问题主线、观点线索和内容价值；
- `content_method_asset`：钩子、结构、节奏、推进、案例位置和 CTA 方法。

同行身份、经历、案例、公司业务、专属数据和识别性原句不得迁移。

### 7.4 显式对标

用户本轮提供的文章、链接或本地参考属于显式对标，不等同于知识库 04。它们需要独立抓取、保真快照和逐篇深度拆解。

---

## 8. 受控检索顺序

### 8.1 总顺序

```text
知识库和 IP 解析
→ 05 IP 核心锚点
→ 03 业务候选
→ 04 内容与方法候选
→ 显式对标深度拆解
→ Gate A
→ 对冻结候选做最终少量片段投影
→ 唯一 Context Pack
```

### 8.2 05 两段式读取

有 IP 时：

第一段在 Gate A 前，只读取最小 IP 锚点：

- 身份与账号定位；
- 目标读者；
- 核心立场；
- 表达风格和禁区；
- 业务方向。

第二段在 Gate A 后，从同一个 IP 中只取与已批准方向相关的少量逐字片段：

- 原话；
- 故事；
- 近期判断或动作；
- 与本篇有关的 CTA 和承诺边界。

无 IP 时完全跳过 05。

### 8.3 IP 资料不足

IP 状态：

- `full`：核心锚点和本篇语料均可用；
- `limited`：只使用已有确认片段，向用户提示但继续；
- `unused`：没有可靠 IP 内容，本篇按无 IP 写法继续。

允许生成明确标注的待补充建议，不允许把虚构经历、案例、数字和第一人称事实写进可发布正文。

### 8.4 03 检索预算

- 根据选题、IP 锚点和用户想法生成 `business_context_needs`。
- 最多准备 0—5 张候选。
- 只选真正支持批准方向的局部片段。
- 索引页、泛词命中和不适用页面不能凑数。
- Gate A 后不得扩大为全库搜索。

### 8.5 04 检索预算

- 根据选题、IP 和相关 03 内容寻找适合的表达机制。
- 最多 0—3 张同行内容资产。
- 最多 0—2 张内容方法资产。
- 无高相关结果时允许为空。
- Gate A 后冻结，不重新搜索、替换或追加。
- 飞书04允许在 Manifest 指定根节点内受限向下两层，以覆盖“分类目录→内容资产”；不得遍历第三层或跳出授权根。03仍只读取直接子级。

### 8.6 读取日志

Runtime 必须记录 `retrieval_receipt.json`，至少包含：

- Run、知识库、IP；
- 来源角色 05/03/04/reference；
- 稳定对象引用；
- 候选和最终选中状态；
- 选中用途和未选原因；
- 读取片段数量与字符量；
- 是否进入 Context Pack。

该收据是运维证据，不是第二份 Writer 上下文。

---

## 9. 显式对标与深度拆解

### 9.1 输入

- 0—5 篇公众号文章、本地 Markdown、用户粘贴文本或受支持来源。
- 每篇独立准备，不先拼接。
- 来源无法完整读取时必须标记，不能把标题或摘要冒充全文。

### 9.2 深度拆解必须包含

每篇至少分析：

1. 标题机制和点击矛盾；
2. 第一屏钩子；
3. 目标读者和核心承诺；
4. 完整结构与每部分功能；
5. 冲突如何建立；
6. 论证顺序和证据安排；
7. 案例、数字和细节的作用；
8. 转折与段落衔接；
9. 情绪和节奏；
10. 结尾与 CTA；
11. 可迁移机制；
12. 禁止迁移的身份、事实、原句和承诺。

多篇参考还必须说明共同机制、差异和真实冲突处理。

### 9.3 无显式对标

没有显式对标不阻断。系统使用 05、03、04 和用户想法生成方向，不伪造对标分析。

---

## 10. Gate A：方向确认

### 10.1 展示内容

Gate A 必须让用户看懂：

- 本次知识库和 IP；
- 谁在说、讲给谁；
- 核心判断和承诺；
- 为什么现在值得写；
- 使用哪些 05/03/04；
- 对标准备保留什么；
- 哪些内容绝不照搬；
- 文章结构和内容推进；
- 干货型或话题型及理由；
- 与写作类型独立的表达方式、专业判断主线、读者真实处境和用户可执行核验动作；
- 业务连接方式和事实边界。

无方向模式同时展示 3 个候选，用户选择后冻结一版完整方向。

### 10.2 合法决定

- `确认方向`
- `需要修改：<具体意见>`
- `不采用`

模糊回复不视为批准。

### 10.3 冻结内容

批准后冻结：

- 知识库、IP 和 IP 状态；
- 原始选题、用户想法和参考集合；
- 目标读者、核心判断、结构、表达方式、专业判断主线、读者处境和核验动作；
- 已选 03/04；
- 详细对标蓝图；
- 写作类型、必须保留、禁区和业务连接。

---

## 11. Article Context Pack

### 11.1 唯一正式输入

Writer 只能读取一份 `article_context_v1.json`，不得再扫描知识库、打开原路径或生成 Source Pack、Writing Packet、Markdown 副本。

### 11.2 字段范围

建议包含：

```text
run_identity
knowledge_base_identity
ip_identity_and_status
task_input
approved_direction
writer_mode
voice_and_viewpoint
selected_05_profile_context
selected_03_business_context
selected_04_content_assets
selected_04_method_assets
selected_reference_mechanisms
must_keep
must_avoid
fact_and_candidate_boundaries
business_link
save_target_preview
```

### 11.3 角色政策

- 05 只支撑当前 IP。
- 03 只支撑当前业务事实。
- 04 只支撑通用内容与方法。
- 对标只支撑本篇可迁移机制。
- 缺少资料时使用一般性或条件性表达，不补造客户事实。

---

## 12. 正文 Writer

### 12.1 写作类型

第一版只保留两个主类型：

- `ganhuo`：教程、政策、流程、SOP、清单、避坑和知识解释；
- `huati`：观点、现象、争议、职业压力、情绪共鸣和讨论。

转化意图是正文的业务连接字段，不再创建第三个 Writer。

`writer_mode` 只决定正文交付的内容价值，不决定是否有人设或观点。`ganhuo` 也必须使用已批准的 IP 表达方式和专业判断主线，不得因为需要人设而切换成 `huati`。每个主要问题单元至少完成一次“读者真实处境→IP 明确判断→专业解释或证据→用户可执行核验动作”；不要求每个自然段机械重复。

### 12.2 输入输出

- 首稿只读唯一 Context Pack。
- 修改稿额外读取当前正文和一条真实用户意见。
- 输出只包含公众号正文，不含标题、标签、分析、状态和保存说明。

### 12.3 IP 与非 IP

- `full/limited`：只使用 Context Pack 中已确认的 IP 片段。
- `unused/none`：使用品牌或中性写法，不模仿个人口吻，不制造个人经历。
- IP 候选素材只有明确标为假设或占位时才能出现，保存前不得残留为真实叙述。

### 12.4 不设置独立质量检查

主链不调用 AI 味检查、Reviewer 或自动润色。Writer 只在自身职责内确认：

- 没有换题；
- 没有越权事实；
- 没有转移同行身份；
- 没有加入标题和分发内容。

---

## 13. 标题与 Gate B

### 13.1 标题能力

标题模块只读取当前完整正文和 Context Pack 中必要的标题上下文，不重新搜索知识库。

输出：

- 极短标题诊断：目标人群、主判断、点击矛盾；
- Top 3 公众号标题；
- 1 个推荐标题；
- 每个标题与正文主判断一致。

不设置双引擎、评分仲裁和标题 Manifest 链。

### 13.2 Gate B 展示

一次展示：

- 完整公众号正文；
- Top 3 标题；
- 推荐标题；
- 保存目标预览。

### 13.3 合法决定

- `确认正文和标题`
- `需要修改正文：<具体意见>`
- `只修改标题：<具体意见>`
- `使用标题：<当前候选或用户明确标题>`

正文修改继续使用同一 Context Pack；改变知识库、IP、选题或核心方向必须新建 Run。

---

## 14. 保存合同

### 14.1 保存时机

只有 Gate B 明确确认后才能保存。确认动作同时授权本次保存，不增加第三次“是否保存”询问。

### 14.2 保存目标

- 目标来自本次知识库 Manifest 或受控解析结果。
- Obsidian 写入约定成稿目录。
- 飞书写入约定知识空间节点或文档目录。
- 不修改 01—05。

### 14.3 确定性保存

保存由 Runtime Adapter 执行：

1. 校验当前 Run、知识库、IP、正文版本和标题选择；
2. 检查目标边界、同名冲突和占位符；
3. create-only 写入；
4. 回读正文、标题、目标对象和版本；
5. 回读一致才报告 `saved`。

### 14.4 最小安全检查

这不是独立质量检查，只防止产品事故：

- 没有把候选素材冒充确认事实；
- 没有未处理的虚构示例或占位符；
- 没有保存到错误知识库、错误 IP 或越界目录；
- 标题来自 Gate B 已展示候选或用户明确输入。

### 14.5 状态语义

```text
draft_generated ≠ confirmed
confirmed ≠ saved
saved ≠ distribution_generated
saved ≠ draftbox
saved ≠ published
```

---

## 15. 可选全平台分发包

### 15.1 触发

正文保存后，用户明确要求才生成。它不是标准主链的第三个 Gate。

### 15.2 输入

- 已确认并保存的公众号正文；
- Gate B 最终标题；
- Context Pack 中已冻结的少量 IP 分发规则和禁区。

不得重新搜索知识库、修改正文或重新决定公众号主标题。

### 15.3 输出

- 公众号最终标题；
- 小红书：标题、50—100 字导语、标签；
- 视频号：标题、50—100 字导语、标签；
- 抖音：标题、50—100 字导语、标签；
- 朋友圈：一版利他转发文案。

第一版不自动发布。是否把分发包保存回知识库，由用户本次明确要求决定。

---

## 16. Skill 架构

### 16.1 总量

第一版固定 6 个 Skill：1 个公开入口、5 个内部 AI Skill。

### 16.2 `content-gzh-slim`

公开入口，负责：

- 解析日常提示词；
- 调用 Runtime 创建或恢复 Run；
- 展示两次 Gate；
- 调度内部 Skill 和保存 Adapter；
- 返回真实状态和下一步。

不得：拆解、检索语义、写正文、写标题、直接保存或发布。

### 16.3 `content-gzh-analyzer`

内部 Skill，负责：

- 深度拆解 0—5 篇显式对标；
- 融合 IP 锚点、03/04 候选和用户想法；
- 有方向时生成一版完整方向；
- 无方向时生成 3 个候选方向；
- 生成 Gate A 和 Writer 详细蓝图。

不得：创建 Run、全库搜索、写正文、标题或保存。

### 16.4 `content-gzh-context-retriever`

内部 Skill，负责：

- 按 05→03→04 的角色和预算选择最终局部片段；
- 复用 Gate A 冻结候选，不扩大搜索；
- 保持事实、运营设定和候选素材分层；
- 生成唯一 Article Context Pack。

不得：改变方向、调用 Writer、保存或生成第二层 Pack。

### 16.5 `content-gzh-writer`

内部 Skill，负责：

- 根据唯一 Context Pack 写公众号正文；
- 根据用户意见修改当前正文；
- 在 `ganhuo/huati` 中只加载一份模式规则。

不得：搜索知识库、重选 IP、重选方向、写标题、分发或保存。

### 16.6 `content-gzh-headline`

内部 Skill，负责：

- 从当前正文提炼主判断和点击矛盾；
- 生成 Top 3 和推荐标题；
- 按当前 IP 禁区约束标题。

不得：重新读取 Profile、改正文、生成分发包或保存。

### 16.7 `content-gzh-distribution-pack`

保存后的可选内部 Skill，负责全平台文案适配。

不得：修改正文、重新起公众号主标题、搜索知识库、保存、草稿箱或发布。

### 16.8 为什么不再创建更多 Skill

- 知识库定位、检索数量、路径和后端写回适合确定性程序。
- Source Prep 是来源适配器，不是语义人格。
- 保存是受控副作用，不应交给自由写作 Skill。
- 不创建 `content-gzh-reviewer`、`content-gzh-ip-loader`、`content-gzh-save`、`content-gzh-source-pack` 或 `content-gzh-writing-packet`。

---

## 17. 复用与重新实现

| 现有能力 | 决策 | 原因 |
| --- | --- | --- |
| `zsk-router` | 上游复用 | 继续负责建库和 03/04/05 入库，本项目不复制 |
| `content-slim` | 参考架构 | 借用单 Run、受限检索、唯一 Context Pack 和 Gate 思路，不直接调用口播主链 |
| `content-analyzer` | 参考后重建 | 公众号需支持 0 篇对标、IP 前置和长文深度拆解 |
| `content-context-retriever` | 参考后重建 | 检索顺序改为 05→03→04，且知识库/IP 来自提示词注入 |
| `content-writer` | 参考后重建 | 输出从口播稿改为公众号长文，只保留干货/话题两种模式 |
| `gzh-headline` | 提取规则，不直接依赖 | 现有实现依赖全局 ProfileResolver，新系统只允许 Context Pack |
| `shu-distribution-pack` | 提取输出合同，不直接依赖 | 现有实现依赖全局 Profile 和 V1 后处理 |
| `shu-gongzhonghao-v1` | 冻结对照 | 不调用、不修改、不共享状态 |

---

## 18. 非 Skill Runtime

建议模块：

```text
runtime/input_parser.py
runtime/knowledge_base_resolver.py
runtime/manifest.py
runtime/run_store.py
runtime/state_machine.py
runtime/reference_prep.py
runtime/retrieval_search.py
runtime/retrieval_reader.py
runtime/retrieval_receipt.py
runtime/context_store.py
runtime/schema_validation.py
runtime/obsidian_adapter.py
runtime/feishu_adapter.py
runtime/save_service.py
runtime/error_model.py
```

程序负责：

- 路径、对象、权限和数量校验；
- Run、锁、版本和状态；
- 候选检索和局部读取；
- Context Pack 和证据 create-only 保存；
- 飞书/Obsidian 写入与回读；
- 用户响应与内部错误分离。

AI Skill 负责：

- 判断相关性；
- 深度拆解；
- 方向融合；
- 片段用途选择；
- 正文、标题和分发文案生成。

---

## 19. 状态机

第一版状态：

```text
created
→ direction_working
→ waiting_direction
→ direction_approved
→ context_ready
→ draft_working
→ waiting_final
→ final_approved
→ saving
→ saved
→ distribution_optional
```

终止状态：

```text
abandoned
blocked
```

规则：

- 只有两次真人等待：`waiting_direction`、`waiting_final`。
- 机械失败复用当前 Run。
- 改知识库、IP、参考集合、原题或核心方向时新建 Run。
- 标题局部修改和正文局部修改在当前 Run 版本化。
- `saved` 是主链终点；分发不改变正文保存事实。

---

## 20. Artifact 预算

正式 Artifact：

```text
run.json
retrieval_receipt.json
reference_snapshot_vN.json
analysis_vN.json
direction_vN.json
approved_direction.json
article_context_v1.json
draft_vN.md
headline_vN.json
approved_final.json
save_receipt.json
distribution_vN.json（可选）
```

限制：

- Writer 正式输入只有 `article_context_v1.json`。
- `retrieval_receipt` 只做运维证据，不可成为第二包。
- 不创建 Source Pack、Writing Packet、Review Pack 和 P0—P9 Artifact。

---

## 21. 错误与降级

### 21.1 可继续降级

- IP 不存在或资料有限：提示，进入 `limited/unused`，继续。
- 03 无相关内容：使用一般性或条件性表达，继续。
- 04 无相关方法：依靠显式对标或通用写作规则，继续。
- 无显式对标：不做伪拆解，继续。
- 无明确选题：在 Gate A 给 3 个候选方向。

### 21.2 必须阻断

- 知识库引用无法解析或无读取权限；
- IP 同名多目标，无法确认具体对象；
- 读取越过本次知识库或混入另一 IP；
- 显式对标被要求作为全文分析但只拿到标题/摘要；
- Gate 版本错绑或被跳过；
- 唯一 Context Pack 缺失、错绑或重复；
- 保存路径越界、权限不足、同名冲突或回读失败。

### 21.3 用户错误信息

只说明：

- 当前处于哪一步；
- 哪项内容可用、有限或不可用；
- 已有 Run 和产物是否保留；
- 用户下一步唯一动作。

不向普通用户展示哈希、Schema、绝对内部路径和技术堆栈。

---

## 22. 隐私、事实与版权

- 每次读取前核对当前知识库和 IP。
- 不跨知识库检索，不使用历史 Run 的客户内容。
- 05 候选素材不冒充事实。
- 03 业务事实只来自当前知识库的选中片段。
- 对标只迁移机制，不照搬原文表达、身份、案例和专属数据。
- 飞书凭据、用户身份、客户私密路径和原文不得写入 Git。
- 测试证据必须脱敏。

---

## 23. 可观测性

每个真实 Run 至少报告：

- 实际读取的 05/03/04/对标数量；
- 每类候选、选中和写入 Context Pack 的数量；
- 总读取字符量；
- IP 状态 `full/limited/unused/none`；
- Gate 等待次数；
- Context Pack 数量；
- Writer 是否发生知识库二次读取；
- 保存后端、保存结果和回读结果；
- 分发是否请求，发布状态必须为未请求。

这些指标用于判断 Slim 是否重新变重，不用于评价文章好坏。

---

## 24. 复杂度预算

固定上限：

| 项目 | 上限 |
| --- | ---: |
| 公开入口 | 1 |
| 内部 AI Skill | 5 |
| Skill 总数 | 6 |
| 真人 Gate | 2 |
| 正常 Run | 1 |
| Writer 正式 Context Pack | 1 |
| Reviewer | 0 |
| 显式对标 | 5 |
| 03 候选 | 5 |
| 04 同行内容资产 | 3 |
| 04 方法资产 | 2 |
| 当前 Run 主 IP | 1 或 none |

超预算必须先有真实失败证据并获得用户批准。

---

## 25. 测试矩阵

### 25.1 核心真实场景

1. 有 IP + 有明确选题 + 有 1—5 篇对标。
2. 有 IP + 无对标 + 有用户想法。
3. 有 IP + 无明确方向，从知识库产生 3 个候选。
4. 无 IP + 有明确选题。
5. 无 IP + 无明确方向。
6. IP 资料有限，提示后继续且不制造第一人称事实。
7. 同一知识库先用 IP A，再新 Run 使用 IP B，互不串用。
8. 不同知识库使用同名 IP，按本次知识库隔离。
9. Obsidian 保存并回读。
10. 飞书保存并回读。
11. 保存后生成全平台分发包，正文不被修改。

### 25.2 负面测试

- IP 名称同名冲突；
- 03/04/05 越界读取；
- Gate A 后重搜 04；
- Writer 尝试搜索知识库；
- 候选素材被写成确认事实；
- 对标只抓到摘要却宣称全文；
- 修改时切换知识库或 IP；
- Gate B 前保存；
- 保存一半成功一半失败；
- 保存被说成草稿箱或发布；
- 分发包重新改公众号主标题或正文。

### 25.3 反重型化验收

每条正常 Run 必须满足：

- 1 个 Run；
- 1 份 Writer Context Pack；
- 2 次真人 Gate；
- Writer 0 次知识库搜索；
- 没有 Reviewer；
- 读取数量不超过预算；
- 保存后主链结束；
- 没有进入配图、口播、草稿箱或发布。

---

## 26. 分阶段实施计划

### P0：私有仓库与 Master SPEC

目标：建立可信独立仓库，冻结产品边界、Skill 架构、预算和测试矩阵。

Gate：

- 仓库为 Private；
- Master SPEC、Compass、State、P1 阶段卡存在；
- 未实现、未安装、未修改现役系统。

### P1：Runtime 基座与唯一入口

目标：创建 `content-gzh-slim` 骨架、输入合同、RunStore、状态机和 fixture adapter。

Gate：

- 知识库和 IP 是 Run 参数；
- 同 Run 冻结一对知识库/IP；
- 两次 Gate 不可跳过；
- 不读取真实客户资料。

### P2：05→03→04 受控检索与 Analyzer

目标：实现检索预算、读取收据、深度对标拆解和 Gate A。

创建：

- `content-gzh-analyzer`
- 候选检索 Runtime
- reference prep

Gate：

- 05 第一段先读；
- 03/04 不全量读取；
- 逐篇深度拆解完整；
- Gate A 冻结真实展示；
- IP 有限不阻断且不造假。

### P3：唯一 Context Pack

目标：创建 `content-gzh-context-retriever`，把冻结方向和最终少量片段装成唯一包。

Gate：

- 只有一份 `article_context_v1.json`；
- 05/03/04 角色不混淆；
- Gate A 后不重搜 04；
- Writer 尚未创建。

### P4：正文、标题与 Gate B

目标：创建 `content-gzh-writer`、`content-gzh-headline`，生成正文和 Top 3 标题。

Gate：

- Writer 只读唯一 Context Pack；
- 干货/话题只加载一个主模式；
- 不运行质量检查；
- Gate B 一次展示正文、标题和保存预览；
- 尚未真实保存。

### P5：飞书/Obsidian 安全保存

目标：实现两个后端 Adapter、create-only 写入、回读和 save receipt。

Gate：

- Gate B 确认后才保存；
- 01—05 不变；
- 写入和回读一致；
- 保存不冒充草稿箱或发布。

### P6：可选分发包与全链测试

目标：创建 `content-gzh-distribution-pack`，完成真实隔离样本和反膨胀验收。

Gate：

- 分发只在保存后明确请求；
- 正文和公众号主标题不被修改；
- 真实样本覆盖有 IP、无 IP、有限 IP、多 IP 同库和两个后端；
- 复杂度不超预算。

### P7：构建、安装与私有发布

目标：在用户授权后构建运行包、安装到测试 Host、完成独立 Review，再决定是否发布版本。

Gate：

- 构建包不含客户数据；
- 安装后入口可发现；
- 两次连续真实 Run 通过；
- 未经授权不替换 V1；
- GitHub 仓库继续保持 Private。

---

## 27. Definition of Done

第一版完成必须同时满足：

- 用户只面对一个 `content-gzh-slim` 入口；
- 同一套流程可处理不同知识库和不同 IP；
- 有 IP 时 05→03→04 按需读取；
- 无 IP 时不读 05；
- 对标深度拆解达到标题、开头、结构、论证、案例、转折、结尾和 CTA 级别；
- 只有两次真人 Gate；
- Writer 只读一份 Context Pack；
- 正文和标题可修改、可确认；
- 飞书和 Obsidian 至少各有一条真实保存回读证据；
- IP 资料有限时继续且不制造事实；
- 多 IP 同知识库不串用；
- 保存、分发、草稿箱和发布状态不混淆；
- 不修改或依赖现役 V1 运行状态；
- 复杂度和读取预算全部达标。

---

## 28. 当前冻结结论

本 SPEC 已冻结以下方向，后续除非用户明确修改，不再反复讨论：

1. 项目名和公开入口：`content-gzh-slim`。
2. 独立仓库，平行重建，不修 V1；通过发布审计后采用 MIT 并公开。
3. 知识库和 IP 可由提示词注入，也可来自已确认默认；每个 Run 仍冻结明确结果，不永久死绑。
4. IP 只填名字，不写 `05/名字`。
5. 一个知识库可以包含多个 active IP；primary 只是默认，一个 Run 只使用一个选定 IP 或无 IP。
6. 有 IP 时按 05→03→04 顺序受控检索。
7. 05、03、04 都不是全量读取。
8. 显式对标保留深度拆解。
9. 主链只有 Gate A 和 Gate B。
10. 不设置质量检查和 Reviewer。
11. 保存目标可以是飞书或 Obsidian 知识库。
12. 全平台分发包是保存后的可选支线。
13. 配图、口播、草稿箱和发布不进入第一版标准主链。
