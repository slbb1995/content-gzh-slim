# Content GZH Slim Compass

本文件是 Master SPEC 的轻量执行入口，不替代 `CONTENT-GZH-SLIM-SPEC.md`。

## 1. 用户要得到什么

用户使用一个日常提示词，指定知识库、IP、选题、对标和自己的想法。系统在一个 Run 中完成：

已确认默认知识库/IP 可省略填写；一段想法即可开始，对标可不提供。按 Master SPEC 2026-09-07 补充：先由入口 Agent 发现同库目录、语义选择并预览完整同行/方法，保留内容价值；不让客户先学结构卡。内部 `writing_requirements` 控制写法，不能混入正文 `must_keep`。

```text
冻结本次知识库与 IP
→ 有 IP 时先读取少量 05 IP 锚点
→ 按需检索少量 03 业务知识
→ 按需检索少量 04 内容方法
→ 对显式对标文章做深度拆解
→ Gate A：确认方向
→ 生成唯一 Article Context Pack
→ 生成公众号正文和 Top 3 标题
→ Gate B：确认正文与标题
→ 保存回指定的飞书或 Obsidian 知识库
→ 可选封面：一次文字推荐/选择，生成一张并保存
→ 可选生成全平台分发包
→ 结束，不代表发布
```

## 2. 日常入口

```text
请按 content-gzh-slim 执行

知识库：
- 客户知识库名称 / 飞书链接 / Obsidian 路径

IP：
- IP 名称 / 无IP

选题：
-

对标：
-

我的想法：
-
```

`IP` 字段必须明确，值可以是具体名字或 `无IP`；不使用 `05/名字` 这种用户输入。

## 3. 七个 Skill

| Skill | 唯一职责 |
| --- | --- |
| `content-gzh-slim` | 唯一公开入口、Run、两次 Gate 和 Runtime 调度 |
| `content-gzh-analyzer` | 深度拆解显式对标，融合知识候选并生成 Gate A |
| `content-gzh-context-retriever` | 按 05→03→04 受控选择，生成唯一 Context Pack |
| `content-gzh-writer` | 根据唯一 Context Pack 生成和修改公众号正文 |
| `content-gzh-headline` | 根据当前正文与冻结上下文生成 Top 3 和推荐标题 |
| `content-gzh-distribution-pack` | 保存后按需生成全平台分发包 |
| `content-gzh-cover` | 保存后一次文字选风格，生成一张封面并保存 |

## 4. 读取预算

- 05：只解析本次指定的一名 IP；启动时读核心锚点，方向冻结后再取少量本篇相关逐字片段。
- 03：最多准备 5 张相关候选，只选真正支持当前方向的局部片段。
- 04：最多 3 张同行内容资产和 2 张内容方法资产；Gate A 后冻结，不再重搜。
- 对标：0—5 篇，逐篇独立保真准备和深度拆解。
- Writer：只能读取一份 `article_context_v1.json`。

## 5. 两个真人 Gate

- Gate A：确认谁在说、讲给谁、核心判断、结构、对标迁移项、知识库素材和业务连接。
- Gate B：一次确认公众号正文、Top 3 标题、推荐标题和保存目标。

## 6. 完整度优先的开发节奏

每阶段只允许：

```text
开发 → 1 次 Review → 最多 1 轮修复 → 1 次复检
```

- 核心结果可用且无串库、越权、数据损坏、Gate 绕过或敏感信息泄露时，记录非关键问题并继续下一阶段。
- 一轮修复后硬阻断仍存在时，交给用户决定，不继续自动修。
- P1—P6 先跑通完整链；P6 再根据真实任务稳定复现的问题集中修正。
- 不为审美、重构、理论风险和未来兼容反复 Review。

## 7. 不做什么

- 不建库、不入库、不修改客户 01—05。
- 不全量扫描知识库。
- 不运行 AI 味或独立质量检查。
- 不调用旧 V1 主链、旧 P0—P9 或双标题仲裁。
- 不生成内页图、口播、进草稿箱或正式发布；用户选择或委托后可以生成一张封面。
- 不永久绑定客户、知识库或 IP；只冻结当前 Run。

## 8. 当前交付与历史阶段

2026-09-08 封面集成：用户已授权独立 Review、子 Agent 隔离端到端测试、修复、提交推送及本地完整包更新。当前总量为 7 个 Skill，日常无子 Agent 或 Reviewer 链；具体交付与验证状态以 VERSION、project-state.json 及 Git 为准。下文保留历史验收，不代表本次生产内容已获真人批准。

2026-09-07 的一段想法启动改造已完成候选验证，用户已追加授权本轮提交、合并及推送 main；以 Master SPEC 顶部补充和 Git 历史为准。本轮没有更新本机安装、确认真实客户绑定或批准真实内容。下段只保留 P7 的历史记录，不是当前任务授权或待办。

P7 候选 `b188329` 已安装到本仓库 `.agents/skills`，隐私扫描、manifest probe、63 项测试与 6 个 Skill 校验通过。真实 Run 1 的 Gate A 已由用户确认；当前为 `waiting_final`，1 Context、1 Writer 输入、Writer 0 次搜索、0 Reviewer、0 保存，正等待 Gate B。允许后续创建一个飞书测试文档；不写全局 Skill、不复制凭据、不替换 V1、不合并、不发布。
